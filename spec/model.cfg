SPECIFICATION Spec

CONSTANTS
  MetricContexts = {ctx1, ctx2}
  MetricTypes = {gauge, count, histogram}
  AggregatorShards = 1
  MaxBufferSize = 100
  MaxBufferElements = 10
  BufferPoolCapacity = 3
  SenderQueueSize = 3
  MaxCountValue = 5
  ContainerIDValues = {"cid1", "cid2"}
  ExternalEnvValues = {"", "env1"}
  MetricSizes = {1, 5, 10}
  MaxPayloads = 10

INVARIANT
  Inv
